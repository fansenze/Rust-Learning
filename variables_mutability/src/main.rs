fn main() {
    const MAX_POINTS: u32 = 100_100;
    println!("max_points is {}", MAX_POINTS);
    let y = MAX_POINTS / 100;
    println!("y is {}", y);
    let mut x = 5;
    println!("now x is {}", x);
    x = 1111;
    println!("then x is {}", x);
}
